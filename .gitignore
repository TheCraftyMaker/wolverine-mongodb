.devswarm/
.devswarm-temp/
.idea/
docs/superpowers/*
# Implementation plans are versioned: task PRs reference them by path on main
!docs/superpowers/plans/

# Local NuGet feed (packed locally for demo — not for source control)
local-nuget/

# .NET build output
[Bb]in/
[Oo]bj/
artifacts/

# Per-task git worktrees (one isolated working tree per task/agent — see the
# Git & PR Workflow section of the implementation plans)
.worktrees/

# IDE / tooling
.vs/
*.user
